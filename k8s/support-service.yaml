apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-service
  namespace: customer-support
  labels:
    app: support-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: support-service
  template:
    metadata:
      labels:
        app: support-service
    spec:
      containers:
        - name: support-service
          image: support-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_URLS
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ASPNETCORE_URLS
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ASPNETCORE_ENVIRONMENT
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: support-connection-string
            - name: RabbitMQ__HostName
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: RabbitMQ__HostName
            - name: RabbitMQ__UserName
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: rabbitmq-user
            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: rabbitmq-pass
            - name: Services__TicketService
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: Services__TicketService
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 5
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: support-service
  namespace: customer-support
  labels:
    app: support-service
spec:
  selector:
    app: support-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30274
  type: NodePort
